// Placeholder for the scaffold checkpoint. The real admin client
// (createAdminClient, server-only) lands in a later checkpoint.
export const ADMIN_SDK_PLACEHOLDER = '@cesco_valle/identity-auth-sdk/admin';
