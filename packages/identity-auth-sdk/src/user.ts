// Placeholder for the scaffold checkpoint. The real user auth client
// (createUserAuthClient + toCookieEntries, edge-safe) lands in a later checkpoint.
export const USER_SDK_PLACEHOLDER = '@cesco/identity-auth-sdk/user';
